# 캡컷 에이전트 — 트랙 A (Mac M칩, arm64) 기준
# 로컬 Mac에서: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- Stage 1: 무음 컷 점프컷 드래프트 ---
pyCapCut==0.0.3          # CapCut 드래프트(draft_info.json) 생성기. import 이름은 `pycapcut`

# --- Stage 2: 로컬 웹 ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9  # 파일 업로드(멀티파트)

# --- Stage 3: ASR (트랙 A) ---
# ★ mlx-whisper 는 Apple Silicon 전용입니다. Intel Mac/기타는 faster-whisper 로 교체하세요.
mlx-whisper>=0.4 ; platform_machine == "arm64" and sys_platform == "darwin"
faster-whisper>=1.0 ; not (platform_machine == "arm64" and sys_platform == "darwin")

# ffmpeg / ffprobe 는 pip 패키지가 아닙니다 — 시스템에 설치돼 있어야 합니다.
#   Mac:  brew install ffmpeg
