/* 토스페이먼츠 클라이언트 키 설정
 *
 * 1) https://developers.tosspayments.com 가입 후 [내 개발정보]에서 키 발급
 * 2) 테스트 키(test_ck_ 로 시작)를 아래에 붙여넣으면 즉시 테스트 결제창이 열린다.
 *    테스트 키 결제는 실제로 돈이 빠져나가지 않는다.
 * 3) 정식 오픈 시 라이브 키(live_ck_)로 교체한다. (사업자등록 + 토스 계약 완료 후)
 *
 * 주의: 여기에는 "클라이언트 키"만 넣는다.
 *       시크릿 키(sk_)는 절대 프런트엔드/저장소에 넣지 않는다 — 승인 서버에서만 사용.
 */
window.TOSS_CLIENT_KEY = ""; // 예: "test_ck_XXXXXXXXXXXXXXXXXXXX"
